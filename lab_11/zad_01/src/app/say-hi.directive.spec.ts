import { SayHiDirective } from './say-hi.directive';

describe('SayHiDirective', () => {
  it('should create an instance', () => {
    const directive = new SayHiDirective();
    expect(directive).toBeTruthy();
  });
});
