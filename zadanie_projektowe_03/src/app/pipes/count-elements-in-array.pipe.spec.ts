import { CountElementsInArrayPipe } from './count-elements-in-array.pipe';

describe('CountElementsInArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new CountElementsInArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
