import { ShowMissingPeoplePipe } from './show-missing-people.pipe';

describe('ShowMissingPeoplePipe', () => {
  it('create an instance', () => {
    const pipe = new ShowMissingPeoplePipe();
    expect(pipe).toBeTruthy();
  });
});
